Sources for the plugin `plugin.applovinMax.Maio`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.Maio" = {
            publisherId = "com.solar2d",
        },
    },
}
```
